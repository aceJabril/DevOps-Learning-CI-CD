services:
  web:
    image: acejabril/task-2-app
    ports:
      - "5002:5002"
    depends_on:
      - redis

  redis:
    image: redis:latest
    volumes:
      - redis-data:/data

volumes:
  redis-data: